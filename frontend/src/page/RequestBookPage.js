import SideBar from '../component/SideBar';
import RequestBook from './Admin/RequestBook';

const RequestBookPage = () => (
  <div className="flex">
    <SideBar />
    <RequestBook />
  </div>
);

export default RequestBookPage;
